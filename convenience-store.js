function changeEnough(coins, amount) {
    let coins = []
    let amount = 0

    const quarters = coins[0] * 25;
    const dimes = coins[1] * 10;
    const nickels = coins[2] * 5;
    const pennies = coins[3] * 1;

    
}